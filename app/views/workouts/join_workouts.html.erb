<h2> Joining </h2>
    <table border="1">
    <tr>

        <th>
          Workout
        </th>

        <th>
          Leader
        </th>

        <th>
          Time
        </th>

        <th>
          Joins
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>
      <% @list_of_joins.each do |a_join| %>
      <tr>

        <td>
        <%=Workout.where({:id=>a_join.workout_id}).workout%>
        </td>

        <td>
          <%=Workout.where({:id=>a_join.workout_id}).first.username%>
        </td>

        <td>
        <%=Workout.where({:id=>a_join.workout_id}).time%>
        </td>

        <td>
          <%= a_join.joins_count %>
        </td>

        <td>
          <%= time_ago_in_words(a_join.created_at) %> ago
        </td>
        <td>
          <%= time_ago_in_words(a_join.updated_at) %> ago
        </td>

        <td>
          <a href="/workouts/<%= a_join.id %>">
            Who's coming?
          </a>
        </td>
      </tr>
      <% end %>
    </table>


        <td>
        <%=Workout.where({:id=>a_join.workout_id}).workout%>
        </td>

        <td>
          <%=Workout.where({:id=>a_join.workout_id}).first.username%>
        </td>

        <td>
        <%=Workout.where({:id=>a_join.workout_id}).time%>
        </td>

        <td>
          <%= a_join.joins_count %>
        </td>

        <td>
          <%= time_ago_in_words(a_join.created_at) %> ago
        </td>
        <td>
          <%= time_ago_in_words(a_join.updated_at) %> ago
        </td>

        <td>
          <a href="/workouts/<%= a_join.id %>">
            Who's coming?
          </a>
        </td>
      </tr>


<h2> Joining </h2>
    <table border="1">
    <tr>

        <th>
          Workout
        </th>

        <th>
          Leader
        </th>

        <th>
          Time
        </th>

        <th>
          Joins
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>
      <% @list_of_joins.each do |a_join| %>
      <tr>
      <%workout=a_join.workout_id%>
      <%workout_each=Workout.where({:id=>workout})%>
      <td>
      <%=workout_each.workout%>
      </td>
      </tr>
      <% end %>
    </table>


<h2 class="m-3"> Joining </h2>
  <div>
    <table class="table table-bordered m-3" border="1">
    <thead class="thead-dark">
      <tr>

        <th scope="col">
          Workout
        </th>

        <th scope="col">
          Leader
        </th>

        <th scope="col">
          Time
        </th>

        <th scope="col">
          Joins
        </th>

        <th scope="col">
          Created at
        </th>

        <th scope="col">
          Updated at
        </th>

        <th scope="col">
        </th>
      </tr>
      </thead>
      <% @list_of_joins.each do |a_join|%>
      <tbody>
      <% the_workout= Workout.where({:id=>a_join.workout_id}) %>
      <tr>
        <td>
          <%= the_workout.workout %>
        </td>

        <td>
          <%=User.where({:id=>the_workout.owner_id}).first.username%>
        </td>

        <td>
          <%= the_workout.time %>
        </td>

        <td>
          <%= the_workout.joins_count %>
        </td>

        <td>
          <%= time_ago_in_words(the_workout.created_at) %> ago
        </td>
        <td>
          <%= time_ago_in_words(the_workout.updated_at) %> ago
        </td>

        <td>
          <a class="btn btn-outline-success" href="/workouts/<%= the_workout.id %>">
            Who's coming?
          </a>
        </td>
      </tr>
      </tbody>
      <% end %>
    </table>
